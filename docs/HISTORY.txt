Changelog
=========

2.1.0 - Unreleased
----------------

* Don't mark things as annotatable, because now we can assume they are.
  [davisagli]

* Removed dc_metadata.pt from skin layer, because a browser view is used now
  instead.
  [davisagli]

* Removed code for handling recursive license changes from the properties tab,
  which is no longer used in Plone 3.
  [davisagli]

* Removed unused metadata_view
  [davisagli]

* Use viewlets for the byline and citation info rather than overriding templates
  [davisagli]


Previously
-----------

* Initial release
